---
name: 'Sumneko Lint'
decription: ' A GitHub action that lints your Lua code using sumneko lua-language-server. Static type checking with EmmyLua!'
inputs:
  directories:
    description: "The directories to lint"
    required: true
    default: ./
    type: string
  checklevel:
    description: "The diagnostics severity level to fail on"
    required: true
    default: Warning
    type: choice
    options:
      - Error
      - Warning
      - Information
      - Hint
  configpath:
    description: "Path to a luarc.json config file"
    required: false
    type: string
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.directories }}
    - ${{ inputs.checklevel }}
    - ${{ inputs.configpath }}
